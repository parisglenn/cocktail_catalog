<%= form_for(@ingredient_modification) do |f| %>
  <% if @ingredient_modification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_modification.errors.count, "error") %> prohibited this ingredient_modification from being saved:</h2>

      <ul>
      <% @ingredient_modification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
