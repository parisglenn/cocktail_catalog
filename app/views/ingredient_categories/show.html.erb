<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @ingredient_category.name %>
</p>

<%= link_to 'Edit', edit_ingredient_category_path(@ingredient_category) %> |
<%= link_to 'Back', ingredient_categories_path %>

<p>
  <strong>Families:</strong>
  <ul>
  <% @ingredient_category.ingredient_families.each do |f| %>
	<li><%= link_to f.name, f %></li>
  <% end %>
	</ul>
</p>