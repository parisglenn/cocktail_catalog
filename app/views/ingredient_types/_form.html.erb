<%= form_for(@ingredient_type) do |f| %>
  <% if @ingredient_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_type.errors.count, "error") %> prohibited this ingredient_type from being saved:</h2>

      <ul>
      <% @ingredient_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ingredient_family_id %><br>
    <%= f.select :ingredient_family_id, options_for_select(@ingredient_family_dropdown, @ingredient_type.ingredient_family_id), {}, class: 'chosen-select' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
